<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

  <!-- Table public.tenants : registry des tenants (slug → schéma) -->
  <changeSet id="phase4-tenants-table" author="mysaas">
    <preConditions onFail="MARK_RAN">
      <not>
        <tableExists schemaName="public" tableName="tenants"/>
      </not>
    </preConditions>
    <createTable schemaName="public" tableName="tenants">
      <column name="slug" type="TEXT">
        <constraints primaryKey="true" nullable="false"/>
      </column>
      <column name="schema_name" type="TEXT">
        <constraints nullable="false"/>
      </column>
      <column name="display_name" type="TEXT">
        <constraints nullable="false"/>
      </column>
      <column name="created_at" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="now()">
        <constraints nullable="false"/>
      </column>
    </createTable>
    <createIndex indexName="idx_tenants_slug" schemaName="public" tableName="tenants">
      <column name="slug"/>
    </createIndex>
  </changeSet>

</databaseChangeLog>